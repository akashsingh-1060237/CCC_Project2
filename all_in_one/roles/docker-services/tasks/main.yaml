---
- name: Deploy stack from a compose file
  become: yes
  docker_stack:
    state: present
    name: mystack
    compose:
      - "/docker/docker-compose.yaml"
  register: output

- debug:
    msg: '{{ output }}'

- pause:
    prompt: "Enter a secret"
    echo: no